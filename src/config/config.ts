import { ElevatorSystemConfig } from "../types/types";

export const ELEVATOR_CONFIG: ElevatorSystemConfig = {
  floorHeightPx: 110,
  doorOpenTimeMs: 2000,
  floorTravelTimeMs: 500,
};

export const DEFAULT_BUILDING = {
  floorCount: 16,
  elevatorCount: 3
};

export const NEW_BUILDING_CONFIG = {
  floorCount: 2,
  elevatorCount: 1,
};